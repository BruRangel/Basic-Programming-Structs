// Solicita ao usuário para inserir o número de Stacks
let Stacks = prompt("Quantos Stacks você tem?");

// Converte a entrada para um número inteiro e calcula o resultado
let resultado = (parseInt(Stacks) * 2) + 50;

// Exibe o resultado
console.log(resultado);